# 歴史年号ダンジョン（HistoryDungeon） 仕様書

> `Concept.md` を `Templates/Spec_template.md` の項目に落とし込んだ実装仕様。
> 共通規約（MVP分離・asmdef分離・event/Dispose徹底など）は `CoreRules.md` に従い、ここにはこのゲーム固有の事項だけを書く。
> 名前空間は `HistoryDungeon.*`（`CoreRules.md` の `MyGame` を置き換える）。

---

## ゲーム概要

- **ゲーム名**: 歴史年号ダンジョン（仮題） / プロジェクト名 `HistoryDungeon`
- **1行説明**: 教室で先生の解説を聞き、地下ダンジョンで住人の4択問題に答えて、歴史の重要年号を時代ごとに覚えていく学習ゲーム
- **ジャンル**: 学習ゲーム（クイズ／RPG風）
- **想定プレイ時間**: 全15時代を通して約1時間（初版の3時代は約10〜15分）
- **想定プラットフォーム**: PC（横画面・マウスクリック操作）

---

## 画面構成

**画面数と遷移フロー**:

Title → Classroom（時代の解説）→ Dungeon（出題）→ Classroom（次の時代）→ … → Ending → Title を巡回する。
Ending で間違えた問題があれば、Ending → 復習（Dungeon シーンを復習用に使う）→ Ending と寄り道できる。
シーンは `Classroom` と `Dungeon` の2つ（Title と Ending は Classroom シーン内のパネルで表現する）。
時代の進行状況はシーンをまたいで引き継ぐ。

**各画面の役割**:

- **Title**: ダンジョンの床・石壁・旗を背景に、金色のゲーム名・キャッチコピー・「はじめる」ボタンを表示し、下に先生とキャラ7体（先生を中央に左右3体ずつ）が並んで上下にゆれる。「はじめる」ボタンはゆっくり拡大縮小する。押すと最初の時代（縄文）の解説へ進む
- **Classroom**: 黒板に時代名・要点の文章・年表を表示し、先生がコミカルに解説する。クリックで解説を1ページずつ送り、最後まで進むとダンジョンへ移動する
- **Dungeon**: 暗めの地下ダンジョン。キャラが1人ずつ現れ、問題文と4つの選択肢を表示する。選ぶと正誤を表示する
  - 正解: 次のキャラに進む。その時代のキャラを全員クリアすると教室へ戻る
  - 不正解: 正解の年号と一言解説を表示し、同じ問題へ再挑戦する
- **Ending**: 最終時代を終えたときに表示する。タイトルと同じダンジョン背景に、金色の「エンディング」、お祝いの文、下に先生とキャラ7体の行列（上下にゆれる）を表示し、「タイトルへ」ボタンで戻る
  - 間違えた問題が1問以上あるときだけ「間違えた問題を復習する（N問）」ボタンを出す
- **復習**: Dungeon シーンを使い、間違えた問題だけを出題する。正解するまで同じ問題を繰り返し（不正解時は正解と解説を表示）、全問正解するとエンディングへ戻る。復習を終えたエンディングは文言が変わり、復習ボタンは消える

---

## ゲームルール

- **時代クリア条件**: その時代のキャラ全員の問題に正解する（不正解でも再挑戦でき、最終的に全問正解すればクリア）
- **終了条件**: 最終時代（平成）を終えたらエンディングへ進む
- **問題数**: 時代ごとに異なる（重要年号の数に合わせる）
- **選択肢**: 4択。選択肢の並び順は出題のたびにランダムにする
- **スコア・タイマー**: 初版では持たない
- **不正解の扱い**: HP や失敗回数は持たない。ペナルティなしで、正解と解説を見せて再挑戦させる
- **間違えた問題の記録**: 1回のプレイ中だけ持つ（セーブしない）。同じ問題を何度間違えても1件。新しく始める・タイトルへ戻ると空になる。復習中の不正解は記録しない
- **その他**: 初版では途中セーブを持たない。終了するとタイトルに戻る

---

## 素材リスト（既に配置されている素材）

### 画像

- Kenney「Tiny Dungeon」（16×16 ドット絵、CC0＝商用可・クレジット不要）から7枚のうち5枚を使用。`Assets/Project/Images/TinyDungeon/`（ライセンス文書も同梱）
  - 教室: 床（`classroom_floor`）・先生（`teacher`＝ひげの魔法使い）
  - ダンジョン: 床（`dungeon_floor`）・壁（`dungeon_wall`）・旗（`dungeon_banner`）・キャラ18種（`Characters/character_01〜18`。人物とモンスター）
  - すべて `Assets/Project/Images/SpriteBank.asset` にまとめ、各シーンの Launcher から View へ渡す（差し替えは枠にスプライトをドラッグするだけ）
  - 取り込み設定: Sprite・PPU 16・Point フィルタ・非圧縮（ドット絵をくっきり見せるため）
- ダンジョンのキャラの絵は、問題ごとに18種を順番に使い回す（時代ごとに選び始めを7ずつずらし、同じ時代の中では重ならない）。名前（例：縄文ゴブリン）と絵は対応していない。復習中は先頭から順に割り当てる
- 差し替え済み: 教室の床と先生、ダンジョンの床・壁・旗・キャラ。UI（吹き出し、ボタン、黒板）は、シンプルな四角のまま残す方針（2026-09-22 決定）。タイトル・エンディング画面は、ダンジョン背景とキャラの行列で整えた

### 素材の意味の重要な注意点

- 教室・ダンジョンの背景、先生、キャラは、仮素材（単色の四角や文字）で代用できる作りにする
- 最終アートスタイルはドット絵に決定（2026-09-22）。入手はフリー素材・Asset Store から探し、差し替えは先生・教室・ダンジョン背景から始める（ダンジョンのキャラは後回し）。差し替えで全体を作り直さずに済むよう、見た目は View 側に閉じ込める

### フォント

- 日本語表示が必須。Noto Sans JP（SIL OFL ライセンス）を `Assets/Project/Fonts/NotoSansJP.ttf` に配置済み。TMP フォントアセットは `NotoSansJP SDF.asset`（Dynamic）

### 効果音・BGM

- BGM 4曲（タイトル・教室・ダンジョン・エンディング）と効果音5種（はじめる/戻るボタン・ページ送り・正解・不正解・時代クリア）を収録。仮の音として秘書が合成した（外部素材なし）。差し替えは `Assets/Project/Audio/SoundBank.asset` の対応する枠にクリップをドラッグするだけ
- 音は `SoundBank`（ScriptableObject）に集約し、再生は `SoundPlayerView` が行う。鳴らすタイミングは Presenter が決める（Logic は音を知らない）
- 音量は `SoundBank` の SeVolume（0.8）と BgmVolume（0.35）で調整する
- 音声ファイルは `Assets/Project/Audio/SE/` と `Assets/Project/Audio/BGM/`

---

## 問題データ（時代・解説・問題）

時代の追加がデータの追加だけで済むよう、データはコードから分離して持つ。

**1つの時代が持つもの**:

- 時代名（例: 縄文時代）
- 解説ページ（複数。黒板に出す文章）
- 年表の項目（年号と出来事の並び）
- ダンジョンの問題の並び

**1つの問題が持つもの**:

- 出題するキャラの名前
- 問題文
- 正解の選択肢と、誤りの選択肢3つ
- 不正解時に見せる一言解説

**運用**:

- 収録は縄文から平成まで全15時代（昭和は「戦前・戦中」と「戦後」の2つに分けている）、計124問（2026-09-22 時点。縄文6・弥生7・古墳7・飛鳥8・奈良8・平安9・鎌倉〜江戸・明治・昭和2つ各9・大正8・平成8。当初の目標約120問に到達）。データは `Assets/Project/App/EraCatalog.cs`
- 問題と解説は秘書が下書きし、オーナーさんが正誤を確認してから取り込む（難関校向けのため確認は必須）

---

## 数値パラメータ

| 名前 | 値 | 意味 |
| --- | --- | --- |
| 選択肢の数 | 4 | 1問あたりの選択肢 |
| 1時代あたりの問題数 | 時代ごとに可変 | 重要年号の数に合わせる |

その他の数値（演出の長さなど）は実装が進んでから追記する。

---

## ゲーム要素の挙動

### 時代の進行

- 時代を先頭から順に進める。「今どの時代か」「教室かダンジョンか」を持つ
- 教室の解説を最後まで送るとダンジョンへ、ダンジョンの全問クリアで次の時代の教室へ、最終時代を終えるとエンディングへ進む
- 進行の変化は Logic が通知し、Presenter がシーン遷移や画面表示に橋渡しする

### 出題進行

- 現在の時代の問題を、先頭から順に1問ずつ出す。Logic は「何問目か」の番号だけで扱い、キャラの表示との対応付けは Presenter が受け持つ
- 選択肢が選ばれたら正誤を判定して通知する
- 正解なら次の問題へ進み、最後の問題に正解したら時代クリアを通知する
- 不正解なら同じ問題に留まり、正解と一言解説の表示を通知する

### 黒板（教室）

- 時代名、要点の文章、年表を表示する
- クリックで次のページへ進む。最後のページでクリックするとダンジョンへ移る

### ダンジョンのキャラ

- 問題ごとに1人ずつ登場し、問題文を話す
- 正解でコミカルに退場し、次のキャラが入れ替わりで登場する
- 不正解のときは、キャラが正解と一言解説を伝える

---

## 演出のイメージ

- 教室は明るく、ダンジョンは暗めにして対比を出す
- 全体はコミカルでにぎやかな雰囲気（先生とキャラは個性的）
- 正解時は小気味よく、不正解時は落ち込ませすぎず解説へ促す
- 演出の細部は仮素材で動く形を作ってから詰める

---

## ビルド（配布）

- 向け先: macOS（Apple シリコン・Intel の両対応＝Universal、Mono）と、Windows 64ビット（x86-64、Mono）
- 出力先: Mac は `Builds/Mac/HistoryDungeon.app`（配布用 `Builds/HistoryDungeon_Mac_0.1.0.zip`、`ditto` で作成）。Windows は `Builds/Windows/HistoryDungeon/HistoryDungeon.exe`（配布用 `Builds/HistoryDungeon_Windows_0.1.0.zip`、フォルダごと圧縮）。`*_BurstDebugInformation_DoNotShip` は配布しない（Windows の zip には含めていない）
- ビルドに含めるシーン: Classroom（先頭）・Dungeon
- プレイヤー設定: 製品名「歴史年号ダンジョン」、会社名 `HistoryDungeon`、識別子 `com.historydungeon.game`、バージョン 0.1.0、ウィンドウ（1920×1080・リサイズ可・フルスクリーン切り替え可）
- アプリのアイコン: `Assets/Project/Images/AppIcon.png`（1024×1024。先生のドット絵を拡大し、ダンジョンの石壁と床の色を合わせた角丸の正方形。素材はすべて Kenney Tiny Dungeon＝CC0）。差し替えは、同じ場所に画像を置き直して Player Settings のアイコン欄を再設定する
- 未設定: Apple の署名と公証（Ad hoc 署名のみ）、Windows の署名（コード署名証明書なし）
- Windows 版の注意: 署名が無いため、初回の起動時に Windows の SmartScreen が「WindowsによってPCが保護されました」と出ることがある。「詳細情報」→「実行」で起動できる。zip は、展開してから起動する（zip の中から直接起動しない）
- ビルドの手順: Unity Hub で Windows Build Support (Mono) を導入済み（Unity の再起動が必要）。エディタのビルド対象を切り替えてからビルドし、終わったら Mac に戻す

### 配布パッケージ（2026-09-22 決定・自分と身近な人に無料配布）

- 配布先: 決まった相手にファイルを直接渡す（GitHub Releases・共有ストレージ・メール添付など。ダウンロードページは作らない）
- 配布パッケージは `Secretary/Distribution/make_packages.sh` で作る（`Builds/Mac`・`Builds/Windows` のビルドから、`Builds/HistoryDungeon_Mac_<version>.zip`・`Builds/HistoryDungeon_Windows_<version>.zip` を生成）
  - 同梱物: 遊び方（`README.txt`。起動手順・困ったときの案内・使用素材とライセンスの一覧）、`Licenses/`（Kenney・Noto Sans JP・UniTask の各ライセンス全文）
  - Mac 版は、Unity が作ったアプリの実行ファイル名が日本語（製品名）のままだと `codesign --verify` に失敗し、受け取った人の Mac で「壊れている」と出る。スクリプトが実行ファイル名を英字（`HistoryDungeon`）に変え、Finder 上の名前は `CFBundleDisplayName` で日本語のまま保ち、署名をやり直して解決している
- 素材のライセンス（配布可能なことを確認済み）: Kenney Tiny Dungeon（CC0）／Noto Sans JP（SIL OFL 1.1）／UniTask（MIT, Copyright 2019 Yoshifumi Kawai / Cysharp, Inc.）／DOTween（Demigiant の無料版ライセンスに準拠。第三者への再配布条件は `Assets/Plugins/Demigiant/DOTween/readme.txt` のリンク先を確認）
- 注意: 署名・公証が無いため、他の Mac で開くと「開発元を検証できません」と出る。右クリック →「開く」で起動できる。広く配るには Apple Developer Program（有料）の署名・公証が必要

---

## ゲーム固有の完成判定

- [x] 縄文から平成までの全15時代を、タイトルからエンディングまで通してプレイできる
- [x] 不正解のとき、正解と一言解説が表示され、同じ問題に再挑戦できる
- [x] 時代ごとの全問に正解すると、教室に戻って次の時代の解説が始まる
- [x] 最終時代を終えるとエンディングが表示され、タイトルへ戻れる
- [x] 日本語の文字が欠けたり四角に化けたりせず、すべて表示される
- [x] 選択肢の並びが出題のたびに変わり、正解位置が固定されていない
- [x] 問題データの正誤をオーナーさんが確認済みである
- [x] 時代を1つ追加するとき、問題データの追加だけで済む
- [x] エンディングで間違えた問題だけを復習でき、全問正解するとエンディングへ戻る

> 確認日: 2026-09-22（初版の3時代は、オーナーの手動プレイで確認済み。全15時代は、秘書が正解を自動で選ぶ通しプレイで確認）。秘書はボタンの押下をコードから発生させて確認し、オーナーが実際にマウスで通しプレイして問題なしと確認した。

---

## 決めた前提（あとで見直せる）

- シーンは Classroom / Dungeon の2つ。Title と Ending は Classroom 内のパネルで表現する
- スコア・タイマー・セーブは持たない（音は Phase 6 で追加済み）
- 選択肢は毎回ランダムに並べ替える
- 日本語フォントは Noto Sans JP（配置済み）を使う
- 昭和は年号が多いため「戦前・戦中」と「戦後」の2つの時代に分けた（オーナーが「先に進めて」と回答したため、下書きの案のまま採用）

---

## このファイルの更新ルール

- パラメータ・素材・ルールを変えたら、その都度この仕様書へ反映する
- 全ゲーム共通のルールは CoreRules.md が担い、ここにはこのゲーム固有の事項だけ書く
- 変更内容と理由は `Secretary/History.md` に記録する
