# CoreRules: Unity共通ルール

> このテンプレートから作るゲームに共通する「設計の考え方」と「書き方の約束」をまとめたファイルです。
> ゲームごとに変わる中身（素材・ルール・画面・数値）はここには書きません。それらは `Spec.md`（雛形: `Templates/Spec_template.md`）にまとめます。
> 初心者がまず迷わないことを最優先に、ルールは欲張らず最小限にしています。慣れてきたら各プロジェクトで足してください。

---

## 前提とする環境・アセット（必須）

このテンプレートは、次が入っていることを前提に進めます。ここはブレません。

- **Unity 6.3系（`6000.3.x`）** — 目安は 6.3.8（`6000.3.8f1`）。6.3系であれば別の番号でも可（詳細は `Secretary/Secretary.md` の「Unity のバージョン方針（6.3系で開く）」）
- **UniTask** — 待ち時間・タイマー・キャンセルなどの非同期処理に使う
- **DOTween** — アニメーションや演出に使う（無料版で可。後述のとおり View 層だけで使う）
- **uLoop ＋ MCP** — AI から Unity を直接操作し、コンパイル・テスト・プレイ確認を自動で回すための仕組み

レンダーパイプラインや Input System のように **プロジェクトごとに変わる設定** は、ここではなく `CLAUDE.md` の環境設定欄に書きます。

---

## 設計の土台：MVP（パッシブビュー）

コードを役割ごとに3つに分けます。これは特定製品の作法ではなく、保守しやすいアプリ設計の一般的な考え方です。

- **Logic（ロジック）** … ゲームのルール・状態・計算を担当。画面のことは一切知らない、ただの C#。
- **View（ビュー）** … 画面に映す／入力を受け取るだけ。自分では判断や計算をしない「受け身」。
- **Presenter（プレゼンター）** … 上の2つの橋渡し。Logic の知らせを受けて、View に「こう表示して」と伝える。

### 依存の向き

```
View  ←──  Presenter  ──→  Logic
```

覚えることは1つだけで十分です：**Logic は View のことも Presenter のことも知らない**。

- View は「ボタンが押された」とだけ知らせ、その後どうするかは決めない。
- Presenter だけが両方を知っていて、つなぐ。
- 全体を生成して配線する入口（Composition Root）だけが、すべてを組み立てる。

この向きさえ守れば、ロジックを単体でテストでき、見た目を作り直しても壊れにくくなります。

### やってはいけない代表例

- Logic が View のメソッドを直接呼ぶ。→ Logic は「起きたこと」を通知するだけにする。
- View の中にスコア計算やルール判定を書く。→ それは Logic の仕事。

---

## アセンブリ分割（asmdef）

層の分離を“仕組みとして”守るために、コードを asmdef で4つに分けます。こうすると、ルール違反（例：Logic から画面の型を使う）がコンパイル段階で弾かれます。

| asmdef | 中身 | 参照してよい相手 |
|---|---|---|
| `MyGame.Logic` | Logic | UniTask だけ |
| `MyGame.View` | View | UniTask ＋ UI / TMP / DOTween など画面まわり |
| `MyGame.Presenter` | Presenter | `MyGame.Logic` と `MyGame.View` |
| `MyGame.Tests.Editor` | Logic のテスト | `MyGame.Logic` |

`MyGame` の部分はプロジェクト名に置き換えます（例：`Breakout.Logic`）。**Logic が他を参照しない**ことが何より大事です。

---

## フォルダ構成と名前の付け方

層ごとにフォルダを分け、その中を **機能名でそろえます**。たとえば「スコア」機能なら、各層に同じ `Score` フォルダを作ります。

```
Assets/Project/
├── Logic/Score/ScoreLogic.cs
├── Presenter/Score/ScorePresenter.cs
├── View/Score/ScoreView.cs        … 必要なら Prefab や Material も同居
├── App/GameLauncher.cs            … 全部を組み立てる入口（Composition Root）
└── Tests/Editor/ScoreLogicTests.cs
```

- 同じ機能の Logic / Presenter / View が、同じ名前のフォルダで対応づくようにします。
- 名前空間も層に合わせます（`MyGame.Logic` / `MyGame.Presenter` / `MyGame.View`）。
- View 専用の Prefab や素材は、その View のフォルダに一緒に置くと探しやすいです。

---

## 自然言語の依頼をどう振り分けるか

利用者は「スコアを増やす仕組みを作って」のように **やりたいことだけ** を伝えます。クラス名や置き場所は指定しません。AI 側が、上のルールに沿って自動的に決めます。

- 計算・ルール・状態 → **Logic**（`〇〇Logic`、設定値なら `〇〇Config`）
- 表示・入力・ボタン・演出 → **View**（`〇〇View`）
- それらの仲介 → **Presenter**（`〇〇Presenter`）
- 全体の組み立て → `App/GameLauncher.cs`
- Logic のテスト → `Tests/Editor/`

はっきりしない部分は、AI が質問を増やしすぎず **妥当な初期値で進め、最後に「こう決めました」と伝える** 方針にします。

---

## 書き方の約束（最小限）

ここは一般的な C# / Unity の作法です。内容は本テンプレ向けに独自にまとめています。

### 通知はイベントで、後始末を必ず

- ロジックの変化（点が増えた・当たった等）は `event Action<T>` で外へ知らせます。
- `+=` で購読したら、終了時（`Dispose` など）に **必ず `-=` で解除** します。
- 解除できるように、**その場限りの匿名ラムダでは購読しない**（名前付きメソッドを使う）。Presenter は `IDisposable` にして `Dispose()` で全部外します。

### 非同期は UniTask

- 待ち・タイマー・繰り返しは UniTask で書きます。`async void` は使いません。
- キャンセル用の `CancellationToken` を引数で受け取り、不要になったら止められるようにします。

### 演出は View 層だけ（DOTween）

- アニメーションは見た目の話なので Logic には書きません。DOTween は View 層でのみ使います。
- 作った Tween は、オブジェクト破棄のタイミングで止めます（`Kill`）。

### 命名・その他

- 役割が伝わる素直な名前にします（省略しすぎない）。
- 1クラス1役割を心がけ、長くなりすぎたら分けます。
- null が「設定し忘れ」を意味する場面では握りつぶさず、そのままエラーにして早く気づけるようにします。

> Unity 標準ボタンを使わず独自ボタンにする、ライフサイクル関数を避けて明示的に初期化する、といった踏み込んだ流儀もあります。初心者向けの本テンプレでは必須にしていないので、採用するかは各プロジェクトの方針として `Spec.md` 側で決めてください。

---

## 動作確認の仕組み（uLoop / MCP）

このテンプレートの肝は「**直したら、そのつど Unity で確かめる**」ことです。AI は uLoop（MCP 経由）で次を実行できます。

| 目的 | 内容 |
|---|---|
| コンパイル | エラー・警告が無いか確かめる |
| テスト | Logic 用の EditMode テストを走らせる |
| プレイ確認 | プレイモードを開始して動かす |
| ログ確認 | 実行時のエラーログを読む |
| 画面確認 | ゲーム画面のスクリーンショットを撮る |

### 守ること

- 「確認します」と言うだけで済ませず、**実際にコマンドを実行**します。
- 実行したら結果（エラー数・テストの合否件数）を **数字で報告** します。
- 未実行のまま「動くはず」と書きません。やっていない項目は正直に「未実行」と書きます。
- 上記はいずれも **Unity が起動している前提** の操作です。Unity が落ちている／閉じられている場合、**無断で起動・再起動しません**。「Unityを起動しますか？」と確認し、了承を得てから起動します（詳細は `Secretary/Secretary.md` の「大原則：Unity の起動・再起動は必ず確認してから」）。なお **プレイモードの開始・停止は確認不要** です。

新しく `.cs` を作ったら、コンパイルして `.meta` が生成されたのを確認し、`.cs` と `.meta` をセットで扱います（GUID 参照が壊れないように）。

### 動的コード実行（execute-dynamic-code）のハマりどころ

uLoop の `execute-dynamic-code` で Unity の状態を調べる時は、次の2点に注意します。

- セキュリティ制限で `Assembly.GetType("...")` はブロックされます。型の存在確認（例：DOTween が入っているか）は、読み込み済みアセンブリを `GetTypes()` で走査して型名を突き合わせる方法を使います。
- スクリプト内の `Debug.Log` は実行結果のJSONには乗りません（「実行成功」しか返らない）。ログ本文は別途 `uloop get-logs` で読み取ります。

---

## 「ひと区切り」の目安

機能を足すごとに、最低これを満たせば一段落です。

- コンパイルがエラー・警告なしで通る
- Logic のテストが通る
- プレイして実行時エラーが出ず、意図どおり動く
- 依存の向きが守れている（Logic が View / Presenter を知らない／View が Logic / Presenter を知らない）
- 購読の解除（`Dispose` での `-=`）が漏れていない

ゲームごとの追加の合格条件は `Spec.md` 側に書きます。

---

## このファイルの更新ルール

- 全ゲーム共通の約束が変わったときだけ、ここを直します。
- 特定ゲームの事情（素材・ルール・数値）は `Spec.md` に書きます。
- 初心者がつまずかないよう、ルールを増やしすぎず、シンプルさを保ちます。
